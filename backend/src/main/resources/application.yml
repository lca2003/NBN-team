server:
  port: 8080

spring:
  application:
    name: nbn-adfeed-backend
  ai:
    dashscope:
      api-key: ${DASHSCOPE_API_KEY:not-configured}
      chat:
        options:
          model: qwen3-8b
          enable-thinking: false
