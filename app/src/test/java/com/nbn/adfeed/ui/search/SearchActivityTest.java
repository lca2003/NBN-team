package com.nbn.adfeed.ui.search;

import org.junit.Test;

import static org.junit.Assert.assertEquals;

public final class SearchActivityTest {
    @Test
    public void searchActivityLivesInSearchPackage() {
        assertEquals("com.nbn.adfeed.ui.search.SearchActivity", SearchActivity.class.getName());
    }
}
